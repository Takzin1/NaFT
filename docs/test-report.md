# テストレポート

## 方式

- **スモークテスト**: `tests/smoke.test.js`（46アサーション）。Node 18+ の `vm.runInThisContext` でアプリJS（`tests/extract-app-js.py` が naft-app.html から抽出）をグローバル文脈にロードし、`document/window/location` を最小スタブして全ユースケース関数を直接実行する。**依存パッケージゼロ**。
- **構文チェック**: `node --check tests/_app.js`
- 実行: `bash tests/run.sh`

## 最新結果（2026-07-03）

```
syntax: OK
RESULT: 46 passed, 0 failed / tx=13, audit=19
```

## カバレッジ（フロー単位）

| 領域 | 検証内容 | 件数 |
|---|---|---|
| シード/公開 | 10プロジェクト生成、LP/マーケット（承認済み8件のみ）/詳細/ログイン描画 | 5 |
| 市民 | ログイン、全7ページ描画 | 7 |
| 支援 | 2段階確認、**残高減算**、支援額加算、**関連チケット優先発行(rw1)**、台帳記録 | 5 |
| 購入予約 | 数量×3,000円の意思表示記録 | 1 |
| 事業者 | 4ページ描画、編集プリフィル、**下書き→審査提出遷移** | 5 |
| 審査(HITL) | キュー2件表示、モーダル、**承認→approved+信頼スコア70**、各管理ページ、QR照合利用 | 9 |
| 加盟店/金融 | 加盟店描画、**複数地域切替** | 3 |
| 全国 | ダッシュボード、**地域新規作成**、全ページ、設定 | 7 |
| 横断 | CSV行数一致、**新規登録→オンボーディング→初回5,000pt付与の台帳記録** | 4 |

## 未カバー（今後の追加候補 → ISSUES_BACKLOG #9）

異常系（残高不足・未承認プロジェクト支援拒否・重複メール登録・コメントなし却下拒否）／チケット期限切れ遷移／リセット後の再シード／`esc()` のXSS回帰テスト／storage永続化の実環境検証（Node上はメモリフォールバックのため）

## リグレッション運用

すべてのPRは `bash tests/run.sh` の46/46通過が必須（docs/pr-checklist.md）。モジュール分割（Phase A）の際も本テストが受入基準となる。
